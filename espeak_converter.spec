# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(['espeak_converter.py'],
             pathex=['D:\\projects\\espeak_converter'],
             binaries=[],
             datas=[('fb2_parser/lang/ru.mo', './fb2_parser/lang/')],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='espeak_converter',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True )
